import React from 'react';
import styles from './Card.module.css';

const Card = (props) => {
    return (
        <main className={styles.main}>
            <p>I'm a card</p>
        </main>
    )
}

export default Card;